import 'package:flutter/material.dart';

class UserCollectionScreen extends StatelessWidget {
  const UserCollectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TODO: 用户收藏页'),
    );
  }
}
